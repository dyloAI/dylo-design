A small mono chip for status or technical metadata (phase, stack, count).

```jsx
<Badge>DATA</Badge>
<Badge variant="solid">SHIPPED</Badge>
<Badge variant="accent">NEW</Badge>
<Badge variant="muted">v0.3</Badge>
```

Use `accent` at most once per view (it spends the oxide). Keep labels short and uppercase.
