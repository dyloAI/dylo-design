repo: dyloAI/dyld-design
branch: main

## Last sync
date: 2026-08-10T00:00:00Z

### Updated in this project
- Attempted import: the repo's default-branch tree and README could not be read (tree endpoint returned empty/409, README not found) — nothing was imported from it.
- All brand foundation content (tokens, components, guideline cards, brand book) instead came from the attached local codebase `Dylo brand system design-3/`, itself a from-scratch brand build with no external source.
- Added one UI kit (`ui_kits/website/`) authored directly against the token/component system, since no product screens exist in either source yet.

## Screen map
| Project screen | Source |
| --- | --- |
| (none) | `dyloAI/dyld-design` was unreadable at sync time — no screens were built from it. Re-sync once the repo has pushed content. |
