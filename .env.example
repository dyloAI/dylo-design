# Passphrase for the gated brand brief at /brief-de-marca.
# Any long passphrase. It is the only control on that page, so make it long
# rather than clever, and share it with the studios by a separate channel.
# Must also be set in the Vercel project (Production + Preview) or the build
# throws at import.
BRAND_BRIEF_PASSWORD=
